import React from "react";
import ProfileCard from "@/components/ProfileCard";

export default function Page() {
  // server component: auth-dependent UI handled in client component
  return <ProfileCard />;
}
